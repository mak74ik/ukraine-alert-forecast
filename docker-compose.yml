version: '3.8'

services:
  alert-forecast:
    build: .
    container_name: ua-alert-forecast
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - HOST=0.0.0.0
      - DEBUG=False
    volumes:
      - ./alerts_data.db:/app/alerts_data.db
